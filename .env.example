# Netlify / local build environment variables
# Set these in Netlify Dashboard → Site settings → Environment variables

PADDLE_ENVIRONMENT=sandbox
PADDLE_CLIENT_TOKEN=test_your_sandbox_client_token_here
